class AddtocartModel {
      final String id;
      final String image;
      final String title;
      final String color;
      final String size;
      final String price;

      AddtocartModel({
        required this.id,
        required this.image,
        required this.title,
        required this.color,
        required this.size,
        required this.price,
      });
}